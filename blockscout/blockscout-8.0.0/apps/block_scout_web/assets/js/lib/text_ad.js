import $ from 'jquery'
import { showAd, fetchTextAdData } from './ad.js'

$(function () {
  // Disabled - khong hien thi text ads
  // Comment out ads code
  /*
  if (showAd()) {
    fetchTextAdData()
  }
  */
})
